import { ShowingListDirective } from './showing-list.directive';

describe('ShowingListDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowingListDirective();
    expect(directive).toBeTruthy();
  });
});
